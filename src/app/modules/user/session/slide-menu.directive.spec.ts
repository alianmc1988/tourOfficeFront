import { SlideMenuDirective } from './slide-menu.directive';

describe('SlideMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideMenuDirective();
    expect(directive).toBeTruthy();
  });
});
